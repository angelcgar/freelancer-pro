// import { createBrowserClient } from '@supabase/ssr';

// import { envs } from '@/config/envs';

// Cliente para el navegador (React components "use client")
// export const createClient = createBrowserClient(
// 	envs.NEXT_PUBLIC_SUPABASE_URL,
// 	envs.NEXT_PUBLIC_SUPABASE_ANON_KEY,
// );

// Plantilla - Supabase comentado
export const createClient: any = null; // Antes era: SupabaseClient
